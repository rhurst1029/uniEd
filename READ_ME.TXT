STARTUP COMMANDS:
    - INSTALLING REQUIREMENTS: pip install -r requirements.txt

    - STARTING FLASK:
        - Activate virtual environment:
            - python3 -m venv venv
            - virtualenv venv
            - source venv/bin/activate
        -Import flask into virtual environment:
            -export FLASK_APP=universalEd.py

        - RUN: flask run